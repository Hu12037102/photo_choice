<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="@dimen/photochoice_bottom_bar_content_height">

    <View
        android:id="@+id/divider"
        android:layout_width="0dp"
        android:layout_height="1px"
        android:background="@color/photochoice_divider"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvThumbnails"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:clipToPadding="false"
        android:paddingStart="10dp"
        android:paddingEnd="6dp"
        android:layout_marginEnd="4dp"
        android:scrollbars="none"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnPreview"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:listitem="@layout/item_bottom_thumbnail" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/btnPreview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:text="@string/photochoice_preview"
        android:textSize="14sp"
        android:textColor="@color/photochoice_on_surface"
        android:clickable="true"
        android:focusable="true"
        android:paddingHorizontal="14dp"
        android:paddingVertical="6dp"
        android:background="@drawable/ripple_rounded_preview"
        app:layout_constraintEnd_toStartOf="@id/btnDone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/btnDone"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:layout_marginEnd="12dp"
        android:gravity="center"
        android:minWidth="64dp"
        android:textSize="13sp"
        android:text="@string/photochoice_done"
        android:textColor="@color/photochoice_on_accent"
        android:clickable="true"
        android:focusable="true"
        android:paddingHorizontal="12dp"
        android:background="@drawable/bg_btn_done_disabled"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:text="Done (3/9)" />

</androidx.constraintlayout.widget.ConstraintLayout>
